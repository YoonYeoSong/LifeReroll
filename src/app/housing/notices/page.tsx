import Link from "next/link";
import { housingNotices } from "@/data/housing/notices";
import { Disclaimers } from "@/components/housing/Disclaimers";
import { HousingNav } from "@/components/housing/HousingNav";
import { formatWon } from "@/lib/housing/format";
export const metadata = { title: "청약 공고 | 청약핏" };
export default function NoticesPage() { const notices = housingNotices.filter(notice => !notice.isHistorical); return <div className="shell housing-page"><HousingNav /><header className="housing-heading"><p className="housing-kicker">FIXTURE NOTICES</p><h1>청약 공고</h1><p>현재는 엔진 검증을 위한 테스트 fixture 공고입니다. 공식 공고 데이터가 아닙니다.</p></header><div className="notice-list">{notices.map(notice => <article className="housing-card notice-card" key={notice.noticeId}><div><p className="housing-kicker">{notice.housingCategory} · {notice.region} {notice.city}</p><h2>{notice.title}</h2><p>{notice.provider} · 접수 {notice.applicationStartDate} ~ {notice.applicationEndDate}</p></div><div className="type-list">{notice.housingTypes.map(type => <div key={type.typeName}><strong>가능 면적 {type.typeName}</strong><span>분양가 {formatWon(type.price)} · {type.supplyCount}세대</span></div>)}</div><p className="source-line">출처: {notice.sourceName} · 기준일 {notice.sourceDate} · <a href={notice.sourceUrl} target="_blank" rel="noreferrer">원문 공고 링크</a></p></article>)}</div><div className="housing-actions"><Link className="housing-primary" href="/housing/profile">내 조건으로 분석하기</Link></div><Disclaimers /></div>; }
