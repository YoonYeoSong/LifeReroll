"use client";
import Link from "next/link"; import { useState } from "react"; import { generateReroll } from "@/lib/lotto"; import type { GeneratedGames } from "@/lib/lotto/types"; import { LottoGameList } from "./LottoGameList";
function PickCard({title,description,games}:{title:string;description:string;games:number[][]}){return <section className="pick-card"><span className="pick-meta">5 GAMES · ₩5,000</span><h2>{title}</h2><p className="pick-description">{description}</p><LottoGameList games={games}/></section>}
export function Generator(){const [result,setResult]=useState<GeneratedGames|null>(null);const [isGenerating,setIsGenerating]=useState(false);const reroll=()=>{setIsGenerating(true);window.setTimeout(()=>{setResult(generateReroll());setIsGenerating(false)},0)};return <><button className="reroll-button" type="button" onClick={reroll} disabled={isGenerating}>{isGenerating?"REROLLING…":result?"REROLL AGAIN":"REROLL MY LIFE"}</button>{result&&<section className="results" aria-live="polite"><div className="pick-grid"><PickCard title="Historical Pick" description="전체 당첨번호 출현빈도를 가중치로 사용해 생성한 조합입니다." games={result.historical}/><PickCard title="Pattern Pick" description="전체 빈도, 최근 흐름, 번호 Pair, 조합 특성을 종합 평가한 조합입니다." games={result.pattern}/></div><div className="total"><div><span>TOTAL</span><strong>10 GAMES</strong></div><strong>₩10,000</strong></div><p className="disclaimer">통계는 선택의 재미를 더할 뿐입니다. <Link href="/about">How it works</Link>에서 계산 방식을 확인하세요.</p></section>}</>}
