import { LottoBall } from "./LottoBall"; export function LottoGameList({games}:{games:number[][]}){return <ol className="game-list">{games.map((game,index)=><li className="game-row" key={game.join("-")}><span className="game-index" aria-hidden="true">{String(index+1).padStart(2,"0")}</span><div className="balls" aria-label={`Game ${index+1}: ${game.join(", ")}`}>{game.map(number=><LottoBall key={number} number={number}/>)}</div></li>)}</ol>}
